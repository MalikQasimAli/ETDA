from setuptools import setup, find_packages

setup(
    name='etz',
    version='0.1',
    description='A Python package for the analysis of eye-tracking data.',
    author='Qasim Ali',
    packages=find_packages(),
)